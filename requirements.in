# QuantLuna — pip-compile source file
# Genereaza requirements.txt cu: pip-compile requirements.in --generate-hashes
#
# Instalare pip-tools:
#   pip install pip-tools
#   pip-compile requirements.in --generate-hashes -o requirements.txt

# Core
numpy>=1.26,<3
pandas>=2.1,<3
scipy>=1.12,<2
scikit-learn>=1.4,<2

# Trading / exchange
ccxt>=4.2,<5

# Async / networking
httpx>=0.27,<1
websockets>=12,<14

# API
fastapi>=0.111,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.7,<3

# ML / stats
statsmodels>=0.14,<1
hmmlearn>=0.3,<1
optuna>=3.6,<4

# Logging
loguru>=0.7,<1

# Notifications
python-telegram-bot>=21,<22

# Dev / test (not pinned in prod)
# pytest>=8
# ruff>=0.4
# pip-tools>=7
